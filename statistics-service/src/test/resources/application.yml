hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000

spring:
  data:
    mongodb:
      database: money-management
      port: 0

rates:
  url: http://api.fixer.io