spring:
  application:
    name: config
  security:
    user:
      password: ${CONFIG_SERVICE_PASSWORD}
      name: user
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configs
  profiles:
     active: native

server:
  port: 8888