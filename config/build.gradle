dependencies {
    compile('org.springframework.cloud:sspring-cloud-config-server')
    compile('org.springframework.cloud:spring-boot-starter-security')

} repositories {
    maven {
        url 'https://repo.spring.io/libs-milestone'
    }
}